#include <stdio.h>
int main()
{   int x,y;float z;
    z=5.2; //Variable Declaration
    x=50; // Value assignment
    y=40;
    printf("value is %.1f\n",z);
    printf("value is %d\n", x);
    printf("value is %d", y);
    return 0;     // send 0 to OS
}
