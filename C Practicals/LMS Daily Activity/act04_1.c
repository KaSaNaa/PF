#include<stdio.h>
#include<math.h>

int main ()
{
    int array[10], sum=0, count=10;
    printf("Enter 10 numbers: ");
    for (int i = 0; i < count; i++)
    {
        scanf("%d", &array[i]);
        printf("\n");
        sum=sum+array[i];
    }
    printf
                                                                                                                      
}