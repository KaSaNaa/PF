#include<stdio.h>
int main()

{
    int x,y;
    for (x = 1; x < 3; x++)
    {
        for (y = 1; y < 4; y++)
        {
            printf("x is %d y is %d\n", x,y);
        }
        
    }
    return 0;
}